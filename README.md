# Day_17_Subqueries-in-SELECT-derived-tables-inline-views
SQL project focused on using subqueries in the SELECT and FROM clauses to build calculated fields and derived tables. Includes examples of inline views, correlated subqueries, and organizing complex logic with nested queries. 
